                    Создаем таблицу
Заходим в MySQL80 Workbench -> Local instance MySQL80 -> Schemas -> Правой кнопкой мыши по облости(ПКМ)
(Schemas) -> Creale Schema... -> Name (названия нашей схемы (Lecture-1))-> Apply -> Apply -> Finish (База
даных создона (Lecture-1)) -> (ПКМ) Tables выбираем Create Table -> Table Name (задаем название для нашей
таблицы (test)) -> Colume Name (заполняем таблицу при этом первое поле появиться автоматически (idtes)
это будет первичным ключом (для того что бы зделать поле первичным ключом поставить галочку в РК по
умолчанию будит INT и для первичнного ключа ставиться атрибут NN) можно поставить свойство (автоинкримент
(AI) (автоматичски будет увеличиваться счесчик))) -> все остольные поля мы можем задать сами (наша база
данных (test) хранит данные о учинеках) -> у ученика есть имя создаем поля nama (для хранения имени мы используем тип даных (Datatype) (VARCHAR) (в круглых скобках указана длина строчки
45 символов)) устонавливае ограничения для name (имя у студента не должно быть пустым ставим галочку в NN)
-> Для входа на платформу студент исползует логин (login) и пароль (passowrd) СОЗДАЕМ -> login (логин
должен быть уникальным для этого ставим галочку в (UQ)) по той же логике создаем passowrd -> Таблица
создана -> Apply -> прописываеться скрипт проверяем если все устраивает -> Apply -> при выполнения
успешного скрипта будит кнопка Finish и (стоит галочка) Execute SQL Statements -> в левой части экрана где
находяться все схемы обновляет открываем Tables и видим табличку test.

                    Заполняеп таблицу

Таблица создона но она пустая что бы туда что либо добавить делаем следушие.
В левой части экрана где все схемы можно обновить информацию на против SCHEMAS есть значок <> нажимаем
lecture-1 -> Tables -> test (третия кнопка нажав на нее мы выводит все поля нашей таблицы)
idtes       -   сурогатный ключ (поле не как не связоно с содержимой нашей таблицы)
name        -   нажимаем на это поля вводим имя
login       -   также логин 
passowrd    -   и также пароль
Чтобы принять изминения кнопка Apply -> Если все правильно Apply -> Finish и (стоит галочка) Execute SQL
Statements. Поле idtes выстовляеться автоматически это и есть AI.

                    Изменения тоблицы

Добовляем еше одно поля в нашу табличку: для этого наводим на нашу табличку test и нажимаем отвертку
(вторая кнопка). Добовляем поле email -> Apply
Возврощяемся в таблицу test (третия кнопка нажав на нее мы выводит все поля нашей таблицы) и мы видим еще
одно поле email. Добовляем . Создоем второго студента пороль и почта одинаковые -> Apply ->